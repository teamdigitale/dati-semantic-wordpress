/*
 Proxy loader for Schema Editor bundle.
 We embed the built bundle from Test/dist into the plugin assets.
 If needed in future, this can be replaced by a proper build step.
*/

// The Test bundle exposes a global function renderSchemaEditor. We rely on it.
// This file exists to keep naming consistent and provide a hook point if needed.

// No-op placeholder: all logic lives in the bundled index.js copied to assets/js.


